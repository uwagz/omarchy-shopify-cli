{
  "schemaVersion": 1,
  "id": "uwagz.shopify-cli",
  "name": "Shopify CLI",
  "version": "1.0.0",
  "author": "uwagz",
  "license": "MIT",
  "homepage": "https://github.com/uwagz/omarchy-shopify-cli",
  "description": "Shopify CLI dev sessions in the Omarchy bar: see running theme/app dev servers, open previews, editor and admin links, copy URLs, and stop sessions.",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "Panel.qml"
  },
  "barWidget": {
    "displayName": "Shopify CLI",
    "description": "Monitor `shopify theme dev` and `shopify app dev` sessions, open previews and admin links, copy URLs, and stop sessions.",
    "category": "Development",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "refreshIntervalSec": 10,
      "fetchDetails": true,
      "showWhenIdle": true,
      "showLabel": false
    },
    "schema": [
      {
        "key": "refreshIntervalSec",
        "type": "integer",
        "label": "Refresh interval while the panel is closed (seconds)",
        "min": 2,
        "max": 300,
        "step": 1,
        "defaultValue": 10
      },
      {
        "key": "fetchDetails",
        "type": "boolean",
        "label": "Query the Shopify CLI for theme details (name, ID, role)",
        "defaultValue": true
      },
      {
        "key": "showWhenIdle",
        "type": "boolean",
        "label": "Show the icon when no session is running",
        "defaultValue": true
      },
      {
        "key": "showLabel",
        "type": "boolean",
        "label": "Show store name / session count next to the icon",
        "defaultValue": false
      }
    ]
  }
}
